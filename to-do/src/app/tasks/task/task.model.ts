export interface Task {
  id: string;
  name: string;
  // todo: boolean;
  // completed: boolean;
}

export interface NewTaskData {
  id: string;
  name: string;
  // todo: boolean;
  // completed: boolean;
}